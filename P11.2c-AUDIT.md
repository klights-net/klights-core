# P11.2c init disposition map

Q11.2c source digest: `aed04501d94550bb18d46044b21ad246d4e89342b077e38bdecb5065291b7b61`.
The report's seven `retain-now` rows were re-audited symbol by symbol against
P11.2c; they are not blanket-retained.

| Report row / production body | Disposition | Owner evidence |
| --- | --- | --- |
| `init/cleanup.rs`: `run_cleanup_with_flags`, `start_cleanup_cni_rpc_server`, `stop_namespace_containerd_after_cleanup`, `open_cleanup_node_local`, `cleanup_directories_and_network` | Retain the root-only teardown sequence: privilege/config/supervisor construction; CNI RPC lifetime; CRI sandbox→network→filesystem order; and best-effort authority boundaries. The former CRI `list→stop→remove` container recovery is moved to `klights-kubelet::cri::CriClient::cleanup_all_runtime_containers`; its canonical plan filters blank IDs, stops every valid ID before any remove, and fails closed. | Kubelet owner tests `runtime_container_cleanup_stops_every_valid_id_before_removing_any` and `runtime_container_cleanup_is_empty_noop_and_fails_closed`. |
| `init/dataplane.rs`: `local_join_dataplane_metadata`, `local_dataplane_peer_metadata_with_endpoint`, `local_dataplane_identity`, `publish_local_dataplane_metadata_self_heal_with_resource_reads`, `enqueue_worker_dataplane_metadata_outbox` | Move WireGuard identity/key materialization and direct-mode omission to `klights-networking::wireguard::LocalDataplaneIdentity`. Move configured-endpoint precedence, Node `ExternalIP` fallback, internal-IP rejection, validation, and leader registration to `klights-networking::wireguard::publish_local_dataplane_metadata_self_heal`. Root only maps `NodeMode`/process config and serializes boundary payloads/outbox. | Networking owner tests `local_identity_for_direct_mode_does_not_create_a_wireguard_key`, `dataplane_self_heal_uses_node_external_ip_and_never_internal_ip`, and `dataplane_self_heal_is_noop_without_a_configured_or_external_ip`; root explicit/fallback checks remain green. |
| `init/host.rs`: `discover_host_ip`, `print_ready_message` | Retain bounded host-process delegation and readiness presentation. `discover_host_ip` is exactly one call into the existing kubelet node-IP owner; output only formats process-local config. | No branch/retry/cache/poll policy exists in either body. |
| `init/leader_control_stream.rs`: reconnect loop/backoff/ACK/failover | Delete the root implementation. Move supervised join/reconnect/ACK/endpoint rotation to existing `klights-leader-rpc::client::start_worker_control_stream`; root callers now make one direct delegation. | `worker_control_stream_reconnect_delay_is_bounded_exponential` in the leader-RPC owner. |
| `init/mod.rs` | Registration only; remove the deleted stream module. | No production behavior. |
| `init/predicates.rs`: `log_role`, runtime/publish predicates, rootless/worker validation, CA selection, API-server predicate | Retain root role/config interpretation. Each body is a bounded role or process-input selection (including the current rootless capability no-op); none owns a reusable runtime recovery, retry, cache, reconciliation, or external side effect. | Existing role and CA mapping tests preserve leader/worker/control-plane distinctions. |
| `init/recovery.rs`: `run_startup_resource_recovery` | Retain root startup sequencing only: an explicit configured-containerd early return; kubelet reusable-containerd decision; then ordered network, mount, cgroup, root-dir, and CNI recovery. The individual CRI/containerd/cgroup and supervisor-file mechanics remain in their existing canonical owners. Moving the orchestration would introduce a networking/bootstrap reverse dependency. | Existing kubelet containerd/cgroup tests own mechanics; all filesystem actions use the injected supervised executor. |

Generic clock conversion moved from the deleted stream helper to
`klights-supervisor::system_time_epoch_millis`, covered by
`system_time_epoch_millis_saturates_pre_epoch_and_large_instants`. The seven
report rows are now fully disposed; no blanket retention remains.
