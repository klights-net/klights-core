# P11.2b controller-adapter disposition map

This is the active symbol-level P11.2b ledger for
`src/bootstrap/controller_adapters/`. It is based on the exact current 34-file
glob, not the scout's historical body snapshots.

## Report and source baseline

- Q11.2b report: `/tmp/p11-s17-q-reports/Q11.2b.txt`
- report SHA-256: `dc55d318e45cc1ccec6926f79a8453472faa6e7389663cbb1923d27d7893f374`
- current glob: 34 Rust files, equal to the 34 report rows
- re-baselined P11.2a mechanical caller-body digests:
  - `controller_runtime_adapter.rs`: `68f7e72708fa476499068f8fbea9fdc9c24c5acd794f9f07fd3793ab2002f122`
  - `crd_registry_adapter.rs`: `cc348f30ff6bdfc63844e7a6c9048aadb876b44b4f300ec57523eb8b2c81d11d`
  - `cronjob_scheduler_adapter.rs`: `71cf0e89be8fbcc86871c0a53f3731dae1bf3d86275a63593de4826bd289f8b3`
  - `node_taint_manager_side_effect_adapter.rs`: `9ec946157aeddffe3a2aa8325dbf697c37e643dccb6b4ef21fdb7f4de86adecf`

Those four changes are accepted P11.2a typed-watch mechanical caller changes;
the remaining 30 current body digests equal Q11.2b exactly.

## Method disposition

`Calls` counts focused-port calls only. Constructors and test-only helpers are
not behavior owners. A retained row is limited to concrete root assembly or a
single focused delegation; no row relies on its directory location.

| File / production symbols | Calls / branches | Disposition |
| --- | --- | --- |
| `apiservice_side_effect_adapter::{RootApiServiceSideEffectStore::list_apiservices,port}` | 1 / expiry mapping | Retain: a concrete cluster LIST projection for the existing controller store port. |
| `applied_pod_side_effect_adapter::{BorrowedAppliedPodPdbStore,handle_applied_pod_side_effects}` | 1 per store method; one controller call | Retain concrete construction of the five existing controller store capabilities; side-effect, Pod-delete, stale-target, and no-feedback decisions remain `side_effects::applied_pod`. |
| `controller_runtime_adapter::{RootControllerLeaderPort,RootControllerPodPort,RootController{Reconcile,Network,Effect}Port}` | 1 command/query per trait method | Retain: root is the concrete leader-command/Pod/runtime capability assembler. Command-error mapping and authority validation are boundary translation, not reconcile selection. |
| `controller_store_error_adapter::map_controller_store_error` | 0 / error-shape match | Retain the root `anyhow` boundary mapping used by concrete adapters; controller algorithms receive `ControllerStoreError`. |
| `coredns_bootstrap_adapter::{CoreDnsBootstrapAdapter,bootstrap_coredns}` | 1 per concrete store method | Retain typed bootstrap-store/deployment-port assembly; CoreDNS desired-resource and Deployment reconcile policy is `klights_controllers::coredns`. |
| `crd_registry_adapter::{LeaderCrdRegistryRuntime,new_runtime,list_crd_values}` | 1 LIST or positioned WATCH / expiry mapping | Retain the concrete positioned LIST-to-WATCH source; CRD registry/replay policy is `klights_controllers::crd` and `klights-watch`. |
| `cronjob_scheduler_adapter::{LeaderCronJobSchedulerRuntime,new_leader_scheduler}` | 1 LIST, WATCH, or reconcile call / expiry mapping | Retain concrete clock/leader-watch assembly. Scheduling, leases, retries, and cron calculation are `klights_controllers::cronjob_scheduler`. |
| `daemonset_node_side_effect_adapter::{RootDaemonSetNodeSideEffectStore,port}` | 1 / expiry mapping | Retain concrete all-namespace DaemonSet LIST capability; selection is controller-owned. |
| `daemonset_store_adapter` | 1 mutation per trait method | Retain concrete Pod mutation forwarding only. |
| `deployment_store_adapter` | 1 mutation per trait method | Retain concrete Pod mutation forwarding only. |
| `endpoint_mirror_side_effect_adapter::{RootEndpointMirrorStore,port}` | 1 / 0 | Retain one-call endpoints mirror/delete forwarding; mirror policy is `controllers::endpoints`. |
| `gc_delete_adapter::{GcOwnerLifecycleAdapter,GcNonPodFinalizationAdapter}` | 1 controller/native call per owner operation; non-Pod finalizer branch is native delete completion mapping | Retain root concrete command/finalizer capability assembly; Pod paths are always `GcPodDeleteSink`, and non-Pod finalization remains explicitly Pod-rejecting. |
| `gc_resource_store_adapter::{BorrowedGcResourceStore,borrowed_store}` | 1 / error translation | Retain concrete focused GC storage view. |
| `hpa_controller_adapter::{NodeApiHpaMetrics,RootHpaReconcileAdapter,HpaControllerAdapter,reconcile_hpa_with_metrics}` | 1 per focused runtime method; workload-kind branch is an existing HPA runtime boundary | Retain node-RPC metric projection and concrete controller capability assembly; metric evaluation, scaling, status retry, and target validation are `controllers::hpa`. |
| `hpa_side_effect_adapter::{RootHpaSideEffectStore,port}` | 1 / 0 | Retain concrete HPA LIST port. |
| `job_side_effect_adapter::{BorrowedJobSideEffectStore,OwnedJobSideEffectStore,borrowed_store,port}` | 1 / expiry mapping | Retain concrete Job LIST port. |
| `job_store_adapter` | 1 mutation per trait method | Retain concrete Pod mutation forwarding only. |
| `mod.rs` | 0 / 0 | Module registration only. |
| `node_lifecycle_controller_adapter::{DatastoreNodeLifecycleStore,NodeLostPodLifecycleAdapter,run}` | 1 LIST or actor enqueue; runtime assembly | Retain cross-domain datastore and kubelet actor assembly. Node lifecycle timing/eviction policy remains `controllers::node_lifecycle`; actor-owned Pod deletion is preserved. |
| `node_subnet_controller_adapter::{DatastorePeerTopologyProjection,DataplaneHealthAdapter,KubeletNodeReadinessPublisher}` | 1–2 / authority/event mapping | Retain cross-owner concrete datastore/network/kubelet bridge. Dataplane behavior remains networking and Node payload policy remains kubelet. |
| `node_taint_manager_side_effect_adapter::{RootNodeTaintNodeStore,DirectNodeTaintNodeStore,port}` | 1 / 0 | Retain concrete Node LIST bridge. |
| `pdb_side_effect_adapter::{RootPdbSideEffectPort,BoundPdbPort,port}` | 1 / 0 | Retain concrete PDB capability assembly. |
| `pod_reconcile_adapter::{PodReconcileAdapter,PodReconcileStorage,PersistentVolumeReconcileAdapter}` | 1 per focused controller call, except eviction | Move eviction ordering: `PodEvictionAdmissionSink::admit_pod_eviction` now delegates once to `controllers::pdb::reconcile_and_admit_pod_eviction_at`; regression `pdb::tests::combined_eviction_admission_reconciles_namespace_before_reserving_disruption`. Retain root assembly and cross-owner concrete stores; service/stale-target and workload policy stay controller-owned. |
| `replicaset_store_adapter` | 1 mutation per trait method | Retain concrete Pod mutation forwarding only. |
| `replicationcontroller_store_adapter` | 1 mutation per trait method | Retain concrete Pod mutation forwarding only. |
| `resource_quota_admission_adapter::ResourceQuotaAdmissionAdapter` | 1 / error translation | Retain concrete admission-store forwarding; quota validation is controller-owned. |
| `resource_quota_controller_adapter::{ResourceQuotaControllerAdapter,reconcile_resource_quotas_for_namespace}` | 1 per read/write method; one controller call | Retain concrete collection/Pod/status ports; quota calculation and status policy are `controllers::resource_quota`. |
| `resource_quota_side_effect_adapter::{RootResourceQuotaSideEffectPort,BoundResourceQuotaPort,port}` | 1 / 0 | Retain concrete resource-quota capability assembly. |
| `service_account_defaults_side_effect_adapter::{RootDefaultServiceAccountPort,port}` | 1 / 0 | Retain concrete namespace bootstrap command forwarding. |
| `service_pod_side_effect_adapter::{BorrowedServicePodStore,borrowed_store,list_resources}` | 1 per typed LIST / expiry mapping | Retain concrete Service/Endpoints/EndpointSlice reads; selector and stale `targetRef` policy is `controllers::side_effects::service_pod`. |
| `service_reconcile_adapter::FocusedServiceReconcileStore` | 1 per trait method | Retain concrete focused service reconcile storage mapping. |
| `statefulset_store_adapter` | 1 mutation per trait method | Retain concrete Pod mutation forwarding only. |
| `system_identity_adapter::{SystemIdentityGenerator,DeterministicControllerIdentityGenerator}` | 0–1 / test-only deterministic branch | Retain process randomness and the shared controller/native identity concrete implementation; identity policy is owned by the consuming crates. |
| `workload_pod_side_effect_adapter::{BorrowedWorkloadPodStore,OwnedWorkloadPodStore,borrowed_store,port}` | 1 per get/LIST / expiry mapping | Retain concrete ReplicaSet/ReplicationController read bridge; ownership/selector policy is `controllers::side_effects::workload_pod`. |

The map reconciles 34 retained-or-partially-moved current rows: 33 contain
only bounded concrete assembly after the eviction move, and the remaining
`pod_reconcile_adapter` row has the one completed controller-policy relocation
recorded above. Subsequent policy candidates must add an owner test before
their root body is changed.
