# P11.2d networking-bootstrap disposition map

Q11.2d was regenerated from accepted P11.1 core `41d8ebba`; its artifact is
`/tmp/Q11.2d.accepted.txt` with SHA-256
`55b36035776546956c0bfb4ef1f8ff2c198b2584bc0efad23640e820694dcb0e`.

The accepted report's five production-body hashes equal the P11.2c predecessor
core `ca4ac43456a04508e75bcea5059e348d720478b1`. The sole report drift is
`networking/service_routing/tests_unit.rs`: accepted
`baeff4c4aed1d8fff71d316780c23909c6d7102d30fc47ef10561d5a25b1cef3`,
predecessor `47b7c681bad531f3c08bdbe9589340ba5493c3602cf8f30d1612b579894cde82`.
It is the accepted P11.2a mechanical replacement of the deleted root
`LeaderRoutingStateAdapter` with
`klights_networking::service_routing::LeaderRoutingStateSource`; it changes no
routing policy or test expectation.

| Accepted row / symbols | Final disposition | Canonical evidence |
| --- | --- | --- |
| `network_adapters.rs`: `ApiServiceRoutingSyncAdapter`, `network_mode`, `cleanup_config` | Retain as bounded root input/error adaptation. Each method maps a validated process mode or forwards one focused service-routing request; it owns no datapath, peer, service, or endpoint behavior. | `network_adapters` has no networking implementation imports or kernel calls; the concrete owner is `klights-networking`. |
| `networking/boot.rs`: `NetworkBootStores`, `NetworkBoot`, `boot_rootless` | Move to `klights_networking::{NetworkBootStores, NetworkBoot}`. The root phase now supplies focused stores and delegates one boot call. | `NetworkBoot` dispatches the validated root/rootless mode entirely inside `crates/klights-networking/src/network_boot.rs`. |
| `networking/plane.rs`: `NetworkPlane` | Move to `klights_networking::NetworkPlane`. Root-mode subnet allocation, rtnetlink, CNI add/delete, peer boot, health, and shutdown remain in the networking package. | `bootstrap_plane_contract_tests::root_network_plane_is_owned_by_networking` and networking's native suite. |
| `networking/mod.rs` | Delete root re-export/facade test shell. Move focused-port, MTU, capability identity, and shutdown tests to `network_composition::facade::tests`. | `networking_implementations_satisfy_their_focused_ports`, `pod_link_mtu_tracks_selected_cross_node_dataplane`, `accessors_preserve_composed_capability_identity`, and `shutdown_calls_service_cleanup_before_datapath_shutdown`. |
| `networking/service_routing/tests.rs` and `tests_unit.rs`: `FreshServiceInventoryClient`, `inventory_resource`, and four routing-snapshot tests | Delete the root-owned test harness. Its fresh, bounded list/no-per-service behavior is owned by `service_routing::leader_state_source::tests::routing_snapshots_use_leader_fresh_for_every_collection`; payload and EndpointSlice parity remain in `service_routing::routing_state_tests` and `tests_unit`. | The owner test asserts the exact six collection requests and `LeaderFresh` consistency; its `get_resource` implementation is unreachable, proving no per-service GET path. |

The retained root call sites construct the final `Network` only from focused
`datapath`, `peering`, `services`, and `resolver` capabilities. No root
networking path, direct netlink/nft call, VXLAN behavior, or networking policy
remains after this leaf.
